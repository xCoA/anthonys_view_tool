require "spec_helper"

describe AnthonysViewTool do
  it "has a version number" do
    expect(AnthonysViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
